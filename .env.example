# Keep deterministic replay in the API while real runners score its events.
API_DEMO_MODE=true
MODEL_DEMO_MODE=false
ALLOW_TRUSTED_ARTIFACTS=true
POSTGRES_PASSWORD=firstwatch_local_demo
WEB_PORT=3000
API_PORT=8000
